#include <iostream>
using namespace std;

void fungsi (int *);

int main(){
	int c,x=1;
	cin>>c;
	int array[c];
	for(int i=0;i<c;i++){
		cin>>array[i];
		if(array[i]%2==0){
			x=0;
		}
	}
	fungsi(&x);
	
}

void fungsi(int *x){
	if(*x==0){
		cout<<"bil genap ada di array";
	}
	else{
		cout<<"bil genap tdk ada di array";
	}
}
