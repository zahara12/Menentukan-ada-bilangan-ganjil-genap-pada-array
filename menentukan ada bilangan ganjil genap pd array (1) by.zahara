#include <iostream>
using namespace std;

void fungsi (int *);

int main(){
	int c,x=1;
	cout<<" Masukan jumlah angka : ";
	cin>>c;
	int Array[c];
	int *PArray;
	PArray=Array;
	for(int i=0;i<c;i++){
	cout<<" Masukan Angka: ";
		cin>>PArray[i];
		if(PArray[i]%2==0){
			x=0;
		}
	}
	fungsi(&x);
	
}

void fungsi(int *x){
	if(*x==0){
		cout<<"Bilangan genap ada di array";
	}
	else{
		cout<<"Bilangan genap tidak ada di array";
	}
}
